name: Docker Build & Test (SoniTranslate)

on:
  push:
    branches: [ main, master ]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Клонируем репозиторий
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Настраиваем Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver-opts: |
            image=moby/buildkit:buildx-stable-1
            network=host

      # 3️⃣ Кэш Docker-слоёв
      - name: Cache Docker layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      # 4️⃣ Сборка Docker-образа
      - name: Build Docker image
        run: |
          echo "🚧 Building Docker image..."
          docker build \
            --cache-from=type=local,src=/tmp/.buildx-cache \
            -t sonitranslate-test .

      # 5️⃣ Запуск контейнера
      - name: Run container
        run: |
          echo "🚀 Running container..."
          docker run -d -p 7860:7860 --name sonitranslate sonitranslate-test
          sleep 60
          docker ps

      # 6️⃣ Проверка доступности API
      - name: Check API health
        run: |
          echo "🧪 Checking API..."
          curl -f http://localhost:7860/health || \
          (echo "❌ API not responding"; docker logs sonitranslate; exit 1)

      # 7️⃣ Показ логов при ошибке
      - name: Show container logs on failure
        if: failure()
        run: docker logs sonitranslate || true

      # 8️⃣ Отладка через SSH (tmate)
      - name: Debug via SSH (optional)
        if: failure()
        uses: mxschmitt/action-tmate@v3

      # 9️⃣ Очистка контейнера
      - name: Clean up
        if: always()
        run: |
          echo "🧹 Cleaning up..."
          docker rm -f sonitranslate || true
